
const navbarMarginBottom = 0;
const logoSize = 35;

const appConstants = {
  baseUnitDistance: `${((navbarMarginBottom - logoSize) / 2)}px` // This is the distance between the top of the screen and the logo
}

export default appConstants

/**
 *
 * Tests for FontAwesomeIcon
 *
 *
 */

import React from 'react';
import { render } from '@testing-library/react';

// import FontAwesomeIcon from './FontAwesomeIcon';

describe('FontAwesomeIcon', () => {
	it('Expect to not log errors in console', () => {
		render(
			<div></div>
			/* <FontAwesomeIcon /> */
		);
	});
});

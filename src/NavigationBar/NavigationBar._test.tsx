/**
 *
 * Tests for NavigationBar
 *
 *
 */

import React from 'react';
import { render } from '@testing-library/react';

// import NavigationBar, { IProps } from './NavigationBar';

describe('NavigationBar', () => {
	// const props: IProps = {};
	it('Expect to not log errors in console', () => {
		render(<div></div>);
	});
});
